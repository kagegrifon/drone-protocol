# Session: Вкладка PROGRAM — редактор библиотечных программ

**Дата:** 2026-05-14  
**Ветка:** feature/personal-programs  
**Тип:** feat

## Цель

Спроектировать и запланировать реализацию новой вкладки PROGRAM и кнопки Edit в LIBRARY.

## Контекст (обнаруженные проблемы после предыдущей реализации)

1. При переключении от назначенной программы к персональной назначенная команда в меню дрона не исчезает — **пользователь подтвердил, что это удобное поведение, менять не нужно**.
2. Нет возможности редактировать библиотечную программу после создания.

## Результаты

- Проведён дизайн-сеанс с визуальным компаньоном (http://localhost:51731)
- Утверждён дизайн: кнопка **Edit** (жёлтая) рядом с Assign в LIBRARY, новая вкладка **PROGRAM**
- Счётчик инструкций убирается везде (LIBRARY и PROGRAM)
- PROGRAM tab: пустое состояние → последняя редактируемая программа
- Заголовок в PROGRAM: имя + Assign (без Edit, без счётчика) + ссылки на назначенных дронов
- Ниже заголовка: полный редактор инструкций (InstructionBlock + AddInstructionMenu)

## Plan

Написан план реализации:  
`C:\Users\Master\.claude\plans\c-users-master-claude-plans-humming-wad-iterative-octopus.md`

**2 задачи, 2 коммита, 1 файл:**  
`src/ui/editor/ProgramEditor/index.tsx`

## Реализация (продолжение сессии)

Выполнен план из `humming-wad-iterative-octopus.md`:

- Убран счётчик `N instr` из карточек LIBRARY
- Добавлена кнопка **Edit** (жёлтая, `#ffaa00`) к каждой программе
- Расширен тип таба: `'drone' | 'library' | 'program'`
- Добавлен стейт `editingProgramId`
- Реализована вкладка PROGRAM: пустое состояние и полный редактор
- Коммит: `ee6412b feat: Добавить вкладку PROGRAM с редактором библиотечных программ (Фаза 1-2)`
