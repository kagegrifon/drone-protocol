# 2026-05-26 — feat: миграция миссий под атомарную семантику

**Цель:** Привести миссии 1–4 в соответствие с атомарной семантикой действий.

**Результаты:**
- M1, M2: обновлены тексты-подсказки (описывают паттерн 4/6 IF-блоков).
- M3: `sharedLoop` переписан на 6-IF расширенный паттерн с зарядкой; оба дрона нацелены на `mine1` (конфликт намерен).
- M4: `loop1` / `loop2` переписаны на 4-IF базовый паттерн без зарядки; дроны останавливаются при разряде (ожидаемое поведение).
- Добавлены структурные unit-тесты (`missions.test.ts`) для M3 и M4.
- Устаревший формат IF-условий в коде миссий устранён — только `ConditionLeaf { left: FunctionCall }`.
