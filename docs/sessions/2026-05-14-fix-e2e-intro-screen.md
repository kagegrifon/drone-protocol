---
date: 2026-05-14
slug: fix-e2e-intro-screen
---

# Цель

Починить падающие e2e тесты.

# Результат

**Причина:** коммит `e3a4f2f` добавил интро-экран («Press Start»), но тесты написаны до него и сразу искали `[data-testid="mission-card-*"]`.

**Фикс:** добавили функцию `skipIntro()` в `e2e/regression.spec.ts` и `e2e/move-to-picker.spec.ts`, вызываем после `page.goto('/')` в каждом тесте.

**Итог:** все 5 e2e тестов проходят (было 4 из 5 падало).
