# Continuous drone movement — дизайн

**Дата:** 2026-06-23
**Ветка:** fix/jerky-drone-movement
**Статус:** ⚠️ УСТАРЕЛ — реализованная модель (look-ahead 1 шаг + ранний resume)
НЕ РАБОТАЕТ в игре. Актуальное решение — в разделе
«## РЕВИЗИЯ 2026-06-23: буфер N=2 / весь path» в конце файла. Читать его.

## Проблема

Движение дрона дёрганое. История правок и их дефекты:

- **До `6078efc`** — одношаговая модель: `moveTo` делает 1 клетку, сбрасывает
  `path=[]`, возвращает управление (`state=running`). Программа в цикле снова
  вызывает `moveTo` → новый шаг. Даёт точку пересчёта поведения между клетками,
  **но** между шагом и приходом нового `intent` от воркера (~15ms async) дрон
  стоит 1 тик (~100ms) → видимая пауза/дёрганость на каждой клетке.

- **`6078efc`** — попытка look-ahead: убрал `path=[]`, заставил `MovementSystem`
  идти по всему сохранённому пути; `onDroneMoved` шлёт resume только при пустом
  path. Убрал паузу, **но сломал модель**: воркер не опрашивается по пути, дрон
  идёт весь маршрут слепо — игрок не может изменить поведение между клетками
  (поехать заряжаться, сменить шахту). Плюс визуальный «рывок назад».

## Как это решают в играх

Стандарт индустрии (RTS, Factorio) — **path following + replanning**: юнит
хранит путь и движется непрерывно; параллельно по событию/таймеру путь
пере-валидируется и корректируется на лету, **не останавливая движение**.
Особенность Drone Loop: «мозг» дрона — JS в воркере с async-latency ~15ms,
поэтому наивный «шаг → спросить воркер → ждать» даёт паузу, которой в нативных
играх нет.

## Целевая модель

**Дрон движется непрерывно, пока не получит сигнал остановиться.**

Сигнал «стоп» (плавная остановка на клетке) приходит когда:
- следующее действие ≠ `moveTo` (mine / charge / drop), **или**
- `moveTo` с **изменившейся целью** (другая `target`), **или**
- подтверждение курса не пришло к концу шага (cold start / лаг).

Если следующее действие — `moveTo` к **той же цели**, путь продлевается и дрон
**не тормозит** (вычисление быстрее анимации, успевает за время шага).

Критерий «та же траектория» — **по целевой точке** (`targetX/targetY`).
Механика хранения пути — **look-ahead на 1 шаг**: `path` содержит максимум
следующую 1 клетку; после каждого шага driver досчитывает следующую.

## Отправная точка (состояние рабочего дерева)

В рабочем дереве есть незакоммиченные правки, частично откатывающие `6078efc`
к одношаговой модели (`MovementSystem` снова делает `path=[]`; `CodeBehaviorDriver`
шлёт ранний resume всегда). План должен исходить из коммита `6078efc` как базы и
довести до целевой continuous-модели; незакоммиченный промежуточный откат можно
отбросить или переиспользовать как удобнее.

## Архитектура

### 1. MovementComponent
Без структурных изменений. `path: Position[]` снова может содержать следующий
шаг (продлеваемый буфер). `targetX/targetY` — текущая цель moveTo, по ней
сравнивается «та же траектория».

### 2. MovementSystem
Делает один шаг по `path[0]`. После шага:
- `path` сдвигается (`shift`).
- Если после shift `path` **не пуст** (driver успел дописать следующий шаг за
  время текущего) → `progress=0`, `state` остаётся `move`, движение
  продолжается **без паузы**.
- Если `path` **пуст** → известный путь исчерпан. `state=running` (если был
  `move`) → программа получит управление. Дрон стоит ровно на клетке
  (безопасная остановка).

`drone:moved` эмитится после каждого шага (как сейчас).

### 3. CodeBehaviorDriver
Обработка `intent: moveTo(point)`:
- Сравнить `point` с текущими `movement.targetX/targetY`.
- **Та же цель** и дрон в движении → распланировать **следующий 1 шаг** A* от
  текущей позиции к цели и **дописать** его в `path` (не сбрасывать `progress`).
  Дрон не тормозит.
- **Другая цель** или дрон стоит → построить новый путь (как сейчас через
  `planMoveToPoint`), дрон плавно поедет в новом направлении.

Ранний resume на `drone:moved` сохраняется: после шага resume шлётся сразу
(~15ms), воркер успевает вернуть `intent` до конца текущего шага → driver
дописывает следующий шаг в `path` до того, как `MovementSystem` обнаружит
пустой путь. Это и устраняет паузу.

### planMove
Нужна функция планирования **одного следующего шага** к цели (look-ahead):
A* до цели, взять `path[0]` как следующий шаг. Существующий `planMoveToPoint`
строит полный путь — для look-ahead берём из него первый шаг либо добавляем
тонкую обёртку `planNextStep(droneId, target, ...)`.

## Поток данных (типичный шаг, цель не меняется)

```
тик N:  MovementSystem шаг в клетку C → path стал пуст → drone:moved
        ↓ (~15ms ранний resume)
        Driver: resume → воркер
        ↓
        воркер: intent moveTo(same target)
        ↓
        Driver: та же цель → planNextStep → path = [следующая клетка]
тик N+1: MovementSystem: path не пуст → продолжает движение без паузы
```

Гонка: между «path опустел → state=running» и приходом intent. При обычной
latency (~15ms ≪ длительность шага: speed=1 → 1000ms, speed=10 → 100ms) intent
успевает, path продлевается до физической остановки. Если не успел (cold start)
— дрон стоит на клетке, resume придёт следующим тиком (безопасно, без рывка).

## Граничные случаи

- **Cold start / лаг воркера** → дрон останавливается на клетке, ждёт ответа.
  Пауза возможна только при реальном лаге (редко), не на каждой клетке.
- **Смена цели на лету** → приходит `moveTo(другая точка)` → driver строит новый
  путь от текущей клетки → дрон плавно меняет направление со следующего шага.
- **Действие ≠ move** → `state` выставляется (mine/charge/drop), дрон стоит на
  клетке. `onDroneMoved` для не-moveTo не шлёт ранний resume (уже так).
- **Блокировка (drone:blocked)** → путь очищен, `state=running`, ранний resume
  подавлён флагом `blocked` (уже так).
- **Достижение цели** → следующий `moveTo` вернёт пустой путь (уже на месте) /
  программа перейдёт к другому действию.

## Тестирование (TDD)

- **MovementSystem** (unit): шаг при непустом продлённом path → продолжает
  (`state=move`); шаг при пустом path → `state=running`, стоит на клетке.
- **CodeBehaviorDriver** (unit): `intent moveTo(same target)` в движении →
  дописывает шаг, не сбрасывает progress; `moveTo(другая цель)` → новый путь;
  не-move действие → не дописывает; cold start (intent не пришёл) → дрон стоит.
- **planNextStep** (unit): возвращает следующий 1 шаг A* к цели.
- **E2E / визуальная верификация**: запустить игру, дрон с `while(true)
  moveTo(mine)` едет плавно без пауз и без рывков назад; смена цели на лету
  даёт плавный разворот.

## Вне scope (YAGNI)

- Сущность `Path` (траектория из точек, итерация в цикле) — отдельная будущая
  фича.
- Подсветка рассчитанной траектории в игре — отдельная фича (зависит от
  хранения полного пути; в look-ahead модели полного пути нет).
- Физический deceleration (замедление) — на сетке не нужен; «плавность» =
  отсутствие пауз и рывков.

---

# РЕВИЗИЯ 2026-06-23: буфер N=2 / весь path

**Статус:** одобрен пользователем, готов к плану. Заменяет всё выше.

## Что было реализовано и почему провалилось

Реализована модель «look-ahead на 1 шаг» (коммиты `4d46ab0`, `d662fcb`,
`b93a6b4`): `moveTo` дописывает 1 следующий шаг, ранний resume на `drone:moved`.
Затем в рабочем дереве — правка «`moveTo` всегда = ровно 1 шаг» (см.
незакоммиченный WIP, ниже).

**Проверка в реальной игре (код пользователя):**
```js
while (true) {
  while (self.inventory === 0) {
    await self.moveTo(World.mines[0].position);
    await self.mine();
  }
}
```
Наблюдение: **дрон снова идёт с задержками** — шаг на 1 клетку, остановка,
шаг, остановка. Ранний resume НЕ помог.

## Root cause (точно установлен)

1. `ProgramExecutionSystem.update()` вызывает `driver.step()` **только при
   `program.state === "running"`**.
2. Порядок систем: `CollisionSystem → ProgramExecutionSystem → MovementSystem`.
3. Цепочка после шага:
   - Тик N: MovementSystem шагнул → `path` пуст → `state=running`.
   - Тик N+1: ProgramExecutionSystem видит running → `step()` → шлёт `resume`
     воркеру. `pending` ещё пуст. **Дрон стоит.**
   - Тик N+1/N+2: приходит ответ воркера (async, межпоточный `postMessage`) →
     `pending` заполнен.
   - Тик N+2: `step()` читает pending → `intent` → новый шаг. Поехал.
   → **1–2 тика простоя на каждой клетке.**

Ранний resume на `drone:moved` шлёт `resume` чуть раньше, но воркеру ВСЁ РАВНО
нужен ≥1 тик на ответ (граница потоков), а `progress` уже обнулён → дрон
визуально стоит. Опрос происходит **в конце шага** — у воркера 0 тиков форы.

## Почему async-задержку нельзя убрать «в лоб» (исследовано)

- **Синхронный вызов кода / синхронная функция в воркере** — не помогает: «синхронно»
  внутри воркера = синхронно в потоке воркера; сообщение в основной поток всё
  равно идёт через async `postMessage`. `Atomics.wait`+`SharedArrayBuffer` дали бы
  синхронность, но блокируют поток и требуют COOP/COEP — отвергнуто.
- **«Подглядеть» (прогнать код вхолостую)** — нестабильно: код с побочными
  эффектами, двойной прогон рассинхронит состояние. Отвергнуто.
- **Сигнал в MovementSystem напрямую вне тика** — мутация мира из `onMessage`
  возможна, но сообщение приходит на ≥1 тик позже — поздно. Не убирает задержку.
- **Событие «не ждать 17ms»** — воркер уже шлёт intent сразу как посчитал;
  проблема не в скорости воркера, а в том, что опрос начат слишком поздно.

**Вывод:** задержку нельзя убрать, можно только СПРЯТАТЬ — дать воркеру фору в
целый шаг, пока дрон едет уже-известную клетку.

## Решение: буфер пути (весь path, пересчёт хвоста)

**Идея:** `path` держит весь маршрут (даёт плавность + возможность рисовать
траекторию). MovementSystem на каждом шаге `shift()` — пока path непуст, дрон
едет без паузы. После каждого шага driver опрашивает воркер (resume), воркер
исполняет код игрока (пересчёт поведения СОХРАНЁН — это отличие от `6078efc`),
присылает `moveTo` → driver **дописывает хвост пути, НЕ трогая `path[0]`** (клетку,
к которой дрон уже едет с накопленным progress → нет рывка).

Фора: пока дрон проезжает уже-известную клетку (≈10 тиков при speed=1), воркер
успевает прислать следующий intent. Задержка спрятана.

### Семантика снапшота (одобрено пользователем)

Воркер считает следующий шаг, пока дрон физически едет предыдущую клетку →
код видит позицию «на шаг вперёд». Это консистентно (все дроны видят так же),
строгая привязка к физической клетке теряется — пользователь подтвердил, что ок.

### Пересчёт хвоста (Вариант A — одобрен)

При `intent moveTo(target)` к **той же** цели: A* считается **от последней
известной клетки в `path`** (не от позиции дрона) к цели; берётся следующий 1
шаг; дописывается в хвост. `path[0]` (текущий едущийся шаг) **никогда не
перезаписывается** → исключён класс рывков, который кусал в `6078efc`.
A* запускается раз на ШАГ (не на тик), на resume после shift.

### Сигнал «стоп» (сброс буфера)

Любой `intent` ≠ `moveTo`-к-той-же-цели **сбрасывает буфер** (`path=[]`) и
применяет новое:
- другая цель (`targetX/Y !== target`) → новый путь от текущей клетки;
- `mine`/`charge`/`drop` → `state` действия, дрон останавливается на клетке.
Дрон доезжает текущую клетку (`path[0]`), затем действует/разворачивается.

## Компоненты (для плана реализации)

1. **planMove.ts** — функция планирования, дописывающая хвост: A* от конца
   буфера к цели, добавить N шагов до буфера ≤ 2 (или вести «весь путь» —
   решить в плане; поведение идентично, буфер ≥2 достаточно для плавности).
   Решение пользователя: хранить **весь path** (для траектории), пересчитывать
   хвост от первой недостигнутой клетки, `path[0]` не трогать.
2. **MovementSystem.ts** — уже корректен: shift, непустой path → продолжаем
   (`state=move`), пустой → `state=running`. Менять не нужно.
3. **CodeBehaviorDriver.ts** — ветка `intent moveTo`: та же цель → дописать
   хвост (не трогая `path[0]`); другая цель / не-move → сброс буфера + новое.
   Ранний resume на `drone:moved` остаётся (даёт старт форы).

## Открытый вопрос для плана

Высокие скорости (speed=10/20, шаг = 1 тик): буфер N=2 спасает, пока воркер
успевает за 1 тик; иначе деградирует в паузу (крайний случай). Пользователь
указал, что в реальной игре speed=1 (шаг=10 тиков) — основной кейс. Буфер ≥2
покрывает speed=1 с запасом.

## Тестирование (TDD)

- **planMove** (unit): дописывание хвоста от конца буфера, `path[0]` не тронут;
  буфер не превышает лимит; сброс при смене цели.
- **CodeBehaviorDriver** (unit): та же цель → хвост дописан, `path[0]` цел,
  progress не сброшен; другая цель → новый путь; не-move → буфер сброшен.
- **MovementSystem** (unit): уже покрыт (continuous при непустом path).
- **E2E / визуальная верификация В ИГРЕ** (критично — unit-тесты проходили,
  а в игре было сломано): код пользователя выше едет ПЛАВНО без пауз;
  подсветка строки прыгает moveTo↔mine на каждой клетке (пересчёт работает).

## Незакоммиченный WIP в дереве (на момент написания)

Правка `CodeBehaviorDriver.ts/.test.ts`: `moveTo` всегда планирует ровно 1 шаг
(`planNextStep`), убрана ветка `sameTarget`/`planMoveToPoint`. Это промежуточная
single-step модель — она вернула задержку (фора 0). План новой сессии может её
сбросить или взять как базу для буфера. Будет закоммичена как WIP отдельно.
